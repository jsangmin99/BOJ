#include <iostream>
#include <cmath>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    string st ="";
    int n = 0;
    
    cin >> st;
    cin >> n;

    cout << st[n-1];
    
    return 0;
}